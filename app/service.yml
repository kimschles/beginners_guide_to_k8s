apiVersion: v1
kind: Service
metadata:
  name: dvlp-app
  labels:
    app: dvlp-app
    namespace: dvlp-app
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
  selector:
    app: dvlp-app
